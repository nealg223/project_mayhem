<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :email %><br />
    <%= f.text_field :email %> 
  </div>
  <div class="field">
    <%= f.label :login %><br />
    <%= f.text_field :login %>
  </div>
  <div class="field">
    <%= check_box_tag(:get_brochure, checked=true) %>
    <%= label_tag(:get_brochure, "Check here to to get the CA Brochure") %>
  </div>
  <div class="field">
    <%= check_box_tag(:mailing_list, checked=false) %>
    <%= label_tag(:mailing_list, "Sign up for the CA Mailing List") %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

